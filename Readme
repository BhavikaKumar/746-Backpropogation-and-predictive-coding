Description of files

1. utils.py - standard function definition.
2. dataset.py - dataset fetched and processed into training and test set
3. neural_layers.py  - types of layers defined in terms of class and definitions to be used in the neural network
4. cnn.py - predictive coding and brackpropagation NN architecture defined, containing essential functions for training and testing
5. main.py - The main run files, intiallized the dataset and training and testing is done depending on the type of training algothrim selected.

We have tested the algorithms on CIFAR-10 dataset. The data is saved in execl file and the plots are generated using matlab.
